# =============================================================================
# CLOUD-INIT CONFIGURATION - BASIC SETUP ONLY
# =============================================================================
# Minimal cloud-init setup - Vault installation handled by Ansible
# This only performs basic system preparation
# =============================================================================

#cloud-config

package_update: true
package_upgrade: false

# Only install essential packages - Ansible will handle Vault-specific packages
packages:
  - python3
  - python3-pip

final_message: "Basic system setup complete. Vault installation will be handled by Ansible."